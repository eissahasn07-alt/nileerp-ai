import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("🏪 NileERP AI")),
      body: Center(
        child: Text("أهلاً يا باشا 😄 جاهز تكسب فلوس؟"),
      ),
    );
  }
}
